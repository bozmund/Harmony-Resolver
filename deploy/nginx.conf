events {}
http {
  upstream resolver { server api-1:8080; server api-2:8080; }
  server {
    listen 80;
    location / { proxy_pass http://resolver; proxy_http_version 1.1; proxy_buffering off; proxy_read_timeout 10m; proxy_send_timeout 10m; }
    location = /metrics { deny all; }
  }
}
